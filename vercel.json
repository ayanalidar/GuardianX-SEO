{
  "buildCommand": "rm -f .env && npm install && npx prisma generate && npx prisma db push --accept-data-loss && npx next build",
  "framework": "nextjs",
  "functions": {
    "src/app/api/**": {
      "maxDuration": 60
    }
  }
}
