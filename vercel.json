{
  "buildCommand": "prisma generate && prisma db push --accept-data-loss && next build",
  "framework": "nextjs",
  "functions": {
    "src/app/api/**": {
      "maxDuration": 60
    }
  },
  "includeFiles": ["db/**"]
}
